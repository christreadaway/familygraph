Design system source files referenced by CLAUDE_CODE_HANDOFF.md.
These are the original mockups and token definitions used to build the
dashboard's Institutional theme. They are development artifacts, not
runtime dependencies.
